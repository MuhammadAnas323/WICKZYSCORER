{
  "name": "video-source-resolver",
  "description": "Cloud Function to resolve public video stream URLs from web pages",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "agora-access-token": "^2.0.4",
    "axios": "^1.7.0",
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^4.9.0"
  },
  "private": true
}
